# Local port and bind defaults (committed). Override locally via config/ports.local.env (gitignored).
API_HOST=127.0.0.1
API_PORT=8000
POSTGRES_PORT=5432
VALKEY_PORT=6379
COMPOSE_INFRA_BIND=127.0.0.1
COMPOSE_APP_BIND=127.0.0.1
